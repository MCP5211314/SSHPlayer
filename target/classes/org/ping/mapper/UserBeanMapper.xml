<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ping.mapper.UserBeanMapper">
<select id="selectUserNameAndPwd" resultType="userbean">
select user_name,user_password from UserBean where user_name=#{user_name} and user_password=#{user_password}
</select>
</mapper>